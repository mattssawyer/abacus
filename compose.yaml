services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: abacus
      POSTGRES_USER: abacus_app
      POSTGRES_PASSWORD: abacus_app
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
